{
  "name": "indigo-palm-booking",
  "version": "1.0.0",
  "description": "Direct booking system for Indigo Palm Collective",
  "main": "index.js",
  "scripts": {
    "build": "eleventy --input=content/blog --output=blog",
    "dev": "eleventy --input=content/blog --output=blog --serve",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "node-ical": "^0.17.1",
    "stripe": "^14.12.0"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "netlify-cli": "^17.15.2"
  }
}
